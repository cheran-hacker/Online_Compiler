import React from 'react';

const Footer = () => {
    return (
        <footer className="footer">
            <p>© {new Date().getFullYear()} AMYPO COMPILER. Built for developers.</p>
        </footer>
    );
};

export default Footer;
